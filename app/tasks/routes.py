from fastapi import APIRouter

router = APIRouter()


# @router.get("/tasks")
# def read_tasks(background_tasks: BackgroundTasks):
#     background_tasks.tasks
#     return {"message": "Task added to background tasks"}
